describe('data type', () => {
    it('should return data type primitive', () => {
       const name: string = "Mee";
       const balance: number = 1000000;
       const isGood: boolean = true;

        console.info(name);
        console.info(balance);
        console.info(isGood);
    });
});